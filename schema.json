{
    "cookies_str": {
        "type": "sensitive",
        "default": "",
        "hint": "QQ空间登录Cookie（可选，若启用自动刷新则不需要手动填写）。获取方法：浏览器登录 qzone.qq.com，按F12打开开发者工具，在Network中找到任意请求，复制Cookie字符串。"
    },
    "qq_ada": {
        "type": "string",
        "default": "",
        "hint": "QQ 适配器名称，不填则自动获取平台类型为 QQ 的适配器"
    },
    "auto_refresh_cookie": {
        "type": "switch",
        "default": true,
        "hint": "是否启用自动刷新Cookie（通过OneBot获取）"
    },
    "cookie_refresh_interval": {
        "type": "string",
        "default": "2h",
        "hint": "Cookie 周期刷新间隔，如 2h、30m、7200（裸数字按秒，7200 = 2 小时）。填 0 或留空则只在启动时和登录失效时刷新。"
    },
    "cookie_refresh_on_use": {
        "type": "string",
        "default": "10m",
        "hint": "用即刷节流：调用空间功能时，距上次刷新超过该间隔就顺手刷新一次（如 '10m'、'30m'，裸数字按分钟）。保持 Cookie 新鲜又不会每次调用都打扰 OneBot。填 0 则关闭（完全依赖周期刷新和失效自动救）。注意：刷新在后台执行，不会拖慢工具调用。"
    },
    "cookie_self_heal": {
        "type": "switch",
        "default": true,
        "hint": "连接自愈：从 OneBot 获取 Cookie 连续失败到阈值时，自动执行「软重置」（丢弃缓存的适配器引用、重建会话、解除刷新节流、重排退避重试），效果等同于手动重载一次插件。全程在后台进行，不打断正常回复。"
    },
    "cookie_self_heal_threshold": {
        "type": "int",
        "default": 3,
        "hint": "连续失败多少次触发连接自愈（默认 3）。"
    },
    "cookie_self_heal_probe": {
        "type": "string",
        "default": "60s",
        "hint": "软重置后的恢复观察窗口，如 '60s'、'2m'。窗口内恢复就结束自愈，超时才升级到自动重载插件。"
    },
    "cookie_self_heal_reload": {
        "type": "switch",
        "default": true,
        "hint": "自愈观察窗口内仍未恢复时，自动执行一次「重载插件」（完全等同于在 WebUI 点一次重载）。两次自动重载之间有冷却，冷却时间会持久化，避免重载风暴；不中断 QQ 消息。"
    },
    "cookie_self_heal_reload_cooldown": {
        "type": "string",
        "default": "10m",
        "hint": "两次「自动重载插件」之间的最小间隔，如 '10m'、'30m'（裸数字按分钟）。同时也是适配器重启的冷却时间。"
    },
    "cookie_self_heal_restart_adapter": {
        "type": "switch",
        "default": false,
        "hint": "最后手段：关闭「自动重载插件」时，可用「重启 QQ 适配器」真正重建 WebSocket 连接。注意：重启期间该适配器上所有 QQ 消息会短暂中断数秒，所以默认关闭，只在确认其它手段无效时开启。"
    },
    "onebot_action_timeout": {
        "type": "string",
        "default": "5s",
        "hint": "调用 OneBot 动作（如获取 Cookie、取消息）的超时时间，如 '5s'、'10s'。调小可以更快失败并转入后台重试。"
    },
    "master_ids": {
        "type": "string",
        "default": "",
        "hint": "主人QQ号，多个用英文逗号分隔。只有这些QQ号才能命令AI使用QQ空间操作性工具（发说说、评论、点赞等）。"
    },
    "master_check_enabled": {
        "type": "switch",
        "default": false,
        "hint": "是否启用代码层主人权限检查。默认关闭，不建议开启：开启后非主人会话中 AI 的自主空间行为（自发发说说、点赞等）也会被一并拦截，让她难以自主行动。推荐保持关闭，并在 persona 人设中写明主人规则来约束。查看说说、识图等只读工具本来就不受此限制。"
    },
    "timeout": {
        "type": "int",
        "default": 10,
        "hint": "HTTP请求超时时间（秒），用于空间接口请求。"
    },
    "image_download_timeout": {
        "type": "int",
        "default": 20,
        "hint": "图片下载的单次请求超时（秒）。只在网络抖动时重试，链接过期（HTTP 400/403/404）会立即失败并交由续命或降级，不会白等。"
    },
    "temp_dir": {
        "type": "string",
        "default": "data/temp",
        "hint": "临时文件存放目录（用于下载图片）"
    },
    "auto_publish_schedule": {
        "type": "string",
        "default": "",
        "hint": "自动发布说说定时。支持两种格式：\n- Cron表达式，如 '0 8 * * *' 每天8点\n- 间隔表达式，如 '2h'（每2小时）或 '2h/30m'（每2小时±30分钟随机）\n留空则禁用。"
    },
    "auto_comment_schedule": {
        "type": "string",
        "default": "",
        "hint": "自动评论定时，格式同上。"
    },
    "auto_reply_schedule": {
        "type": "string",
        "default": "",
        "hint": "自动回复评论定时，格式同上。"
    },
    "auto_reply_enabled": {
        "type": "switch",
        "default": false,
        "hint": "是否开启自动回复评论（需配置auto_reply_schedule）"
    },
    "task_group_ids": {
        "type": "string",
        "default": "",
        "hint": "用于接收定时任务指令的群号列表（多个用英文逗号分隔）。每次定时任务会随机选择一个群发送指令。"
    },
    "task_private_ids": {
        "type": "string",
        "default": "",
        "hint": "用于接收定时任务指令的个人QQ号列表（多个用英文逗号分隔）。"
    },
    "task_message_style": {
        "type": "string",
        "enum": ["silent", "notify"],
        "default": "silent",
        "hint": "定时任务消息样式：silent=发送指令后不保留（无痕），notify=保留消息（通知大家）"
    },
    "auto_publish_group_id": {
        "type": "string",
        "default": "",
        "hint": "用于获取聊天话题和图片的群号（后台直接生成模式专用，仅当未配置任何任务目标时生效）"
    },
    "auto_publish_user_id": {
        "type": "string",
        "default": "",
        "hint": "用于获取聊天话题和图片的个人QQ号（后台直接生成模式专用，仅当未配置任何任务目标且未配置群号时生效）"
    },
    "auto_publish_image_prob": {
        "type": "float",
        "default": 1.0,
        "hint": "主动发布进入配图候选流程的概率（0-1）。"
    },
    "auto_publish_image_min": {
        "type": "int",
        "default": 0,
        "hint": "定时自动发布每次随机抽取的配图目标下限。抽到0时AI可按内容自主选择0至最大值张；抽到正数时优先按该数量配图，候选不足时允许少图或纯文字降级。"
    },
    "auto_publish_image_max": {
        "type": "int",
        "default": 3,
        "hint": "定时自动发布每次随机抽取的配图目标上限，也是抽到0时AI可自主选择的最多图片数。仅影响定时任务，不影响用户主动发布。"
    },
    "auto_publish_image_fallback": {
        "type": "switch",
        "default": false,
        "hint": "保留配置项（当前版本未接线）：配图选择异常时的兜底行为由「按目标数量尽量补足、候选不足则降级为少图或纯文字」统一处理。"
    },
    "auto_publish_image_dedupe_interval": {
        "type": "string",
        "default": "3d",
        "hint": "主动发布图片去重间隔，如 3d、72h、12h、0（裸数字按小时）。仅发布成功后记录。按图片**内容**判重（链接换签名也算同一张），定时发布与群聊指令模式均生效；用户主动指定图片不受限制。"
    },
    "max_comments_per_cycle": {
        "type": "int",
        "default": 3,
        "hint": "每次自动评论最多评论多少条说说（随机选择）"
    },
    "max_replies_per_cycle": {
        "type": "int",
        "default": 5,
        "hint": "每次自动回复任务最多回复多少条新评论"
    },
    "like_when_comment": {
        "type": "switch",
        "default": false,
        "hint": "评论说说后自动顺带点赞（由插件直接执行，AI 无需再调用点赞工具）。群聊指令模式和后台直接生成模式均生效。"
    },
    "action_interval": {
        "type": "string",
        "default": "0.5-1.5s",
        "hint": "写操作（评论/点赞/回复/删评论）发送前的透明节流范围，如 '0.5-1.5s'。仅错开发送时机，不会拦截或失败，AI 无感知；连续操作被风控时可调大（如 '2-4s'）。"
    },
    "like_delay": {
        "type": "string",
        "default": "0.5-1.5s",
        "hint": "评论后自动点赞前的随机延迟范围，如 '0.5-1.5s'。错开'评论+点赞'的连续请求特征。"
    },
    "comment_verify": {
        "type": "switch",
        "default": false,
        "hint": "评论提交后回读确认（诊断模式，默认关）：提交成功后延迟再拉一次说说详情，确认评论是否落库，结果只写日志不阻断判定。排查'评论成功但没显示'时临时开启。"
    },
    "qzone_blacklist": {
        "type": "string",
        "default": "",
        "hint": "QQ空间操作黑名单（QQ号，多个用英文逗号分隔）。禁止对填写的空间进行任何操作（包括查看），返回拒绝提示。黑名单优先于白名单，填了自己也禁止自己。"
    },
    "qzone_whitelist": {
        "type": "string",
        "default": "",
        "hint": "QQ空间操作白名单（QQ号，多个用英文逗号分隔）。只允许对填写的空间进行操作（自己始终允许，除非进黑名单）；留空则除黑名单外全部允许。"
    },
    "like_users_display_max": {
        "type": "number",
        "default": 5,
        "hint": "查看说说时展示点赞人昵称的数量上限（默认 5，模拟空间页「xx等人觉得很赞」；总数不受影响，只影响展示几个昵称）。"
    },
    "backend_llm_model": {
        "type": "model_select",
        "model_type": "llm",
        "default": "",
        "hint": "「后台直接生成模式」使用的LLM模型。留空则使用默认快速模型。"
    },
    "backend_persona": {
        "type": "persona_select",
        "default": "",
        "hint": "「后台直接生成模式」使用的人设。留空则跟随 WebUI 当前激活的人设（切换激活人设即时生效，无需重载插件）。"
    },
    "blackout_schedules": {
        "type": "list",
        "default": [],
        "hint": "禁止执行定时任务的时间段列表。格式：'HH:MM-HH:MM'，支持跨天。例如：['22:00-06:00', '12:00-13:00']。仅对定时任务生效，用户主动触发的指令不受此限制。"
    },
    "visitor_limit": {
        "type": "int",
        "default": 20,
        "hint": "访客统计最多返回的最近访客条数（1-50）。统计数字仍按QQ空间接口返回的今日/最近30天总数展示。"
    },
    "image_manifest_enabled": {
        "type": "switch",
        "default": true,
        "hint": "是否向 AI 注入「近期图片清单」（含每张图的内容描述），让她发说说配图时知道图片实际内容。描述复用全局限流缓存，同一张图只识别一次。"
    },
    "image_manifest_count": {
        "type": "int",
        "default": 5,
        "hint": "近期图片清单包含的图片数量（每会话）。"
    },
    "manifest_inject_mode": {
        "type": "string",
        "enum": ["on_demand", "always"],
        "default": "on_demand",
        "hint": "图片清单注入策略。on_demand（默认）= 只在插件自己的定时发布任务那一轮注入（那是唯一确定「她这就要发说说、并且需要挑图」的时刻）；用户在聊天里让她发说说不需要清单——她自己能从消息文本里看到图片路径，真需要清单时空手调用 qzone_publish 也会按需返回。always = 旧行为（该会话有可用候选就每轮注入）。注意：清单只列「框架已经处理过」的图片（含 native 模式随消息发出的图），没处理过的不列、不下载、也不额外识图。"
    },
    "image_desc_max_chars": {
        "type": "int",
        "default": 80,
        "hint": "清单/提示词里每张图描述的展示长度上限（默认 80 字，超出截断并加省略号；填 0 = 不截断）。识图模型对「图片里有文字」会把文字全部输出，截图类描述可能几百字，而这段文本是按张数进 prompt 的，所以建议保留上限。"
    },
    "qzone_image_desc_enabled": {
        "type": "switch",
        "default": true,
        "hint": "是否启用空间说说配图识别（qzone_describe_image 工具）。AI 会根据说说语义自行决定是否识图。"
    },
    "qzone_image_desc_own": {
        "type": "switch",
        "default": false,
        "hint": "是否允许 AI 对自己空间的说说调用识图。默认关闭（配图本来就是她自己选的，通常没必要）。"
    },
    "auto_comment_image_desc": {
        "type": "switch",
        "default": false,
        "hint": "「后台直接生成模式」的自动评论是否先识别对方说说配图再评论（仅对带图说说生效）。"
    },
    "image_desc_model": {
        "type": "model_select",
        "model_type": "llm",
        "default": "",
        "hint": "图片描述使用的模型。留空则使用默认 VLM 模型。"
    },
    "image_fetch_fail_ttl": {
        "type": "string",
        "default": "10m",
        "hint": "图片失效/识图失败后的静默时长，如 '10m'、'30m'（裸数字按分钟）。这段时间内不再重复下载、不再刷日志（过期图片不会每轮都报错），到期后会自动再试一次。"
    },
    "auto_attach_recent_image": {
        "type": "switch",
        "default": false,
        "hint": "吸附模式：发说说未指定图片时，AI会抓最近聊天中的一张图配上，配图概率更大但她不知道图片真正内容；图失效或抓不到时会自动降级为纯文字发布，不会导致发送失败。默认关闭，开启后会取代AI通过近期图片清单知情选图的行为。"
    }
}
